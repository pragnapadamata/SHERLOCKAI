# Base URL for the backend API.
#   empty  -> same origin: the Vite dev proxy (dev) or the single-origin
#             StaticFiles build served by FastAPI (recorded demo).
#   value  -> e.g. https://api.example.com for a split deploy (frontend on Vercel).
VITE_API_BASE=
